# FloodGuard BD

Professional Bangladesh flood-intelligence dashboard using Flask, Chart.js and Leaflet.

Official FFWC observed water-level data is fetched best-effort from public FFWC pages and labelled as live only when successfully matched. The dashboard also supports clearly labelled fallback reference values.

Run with `run.bat`; the app uses port **5058**.
